apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - gitrepository.yaml
  - helmRelease.yaml
  - gatekeeper-secret.yaml
patches:
  - target:
      kind: HelmChart
      name: Chart.yaml
    patch: |-
      - op: remove
        path: /dependencies/0